import type { User, Ticket, Draw } from './types';

// This file is now for types and can be used for mock data during testing if needed.
// The main application will now fetch data from Firebase.

export const users: User[] = [];
export const draws: Draw[] = [];
export const tickets: Ticket[] = [];
